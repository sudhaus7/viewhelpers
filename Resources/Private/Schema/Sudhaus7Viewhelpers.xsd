No command could be found that matches the command identifier "schema:generate".

Enter "/usr/local/bin/composer typo3/cli_dispatch.phpsh extbase help" for an overview of all available commands
or "/usr/local/bin/composer typo3/cli_dispatch.phpsh extbase help <command identifier>" for a detailed description of the corresponding command.
